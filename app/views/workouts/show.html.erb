
    <h3><%= @workout.name %></h3>

</br>
<!-- <p>
    <% if @workout.exercises.count === 0 %>
      <%= "This workout has no exercises, add some below" %>
    <% else %>
    <ol>
      <% @workout.exercises.all.each do |exercise| %>
        <li><%= exercise.name %></li>
        <% end %>
    </ol>
    <% end %>
</p> -->

<div class="workout_exercises">
  <ol>
    <% @workout.exercises.all.each do |exercise| %>
      <li><%= exercise.name %></li>
      <% end %>
  </ol>
</div>

  <%= link_to "add exercise", new_workout_exercise_path(@workout), :class => "add_exercise"%>


<div align="center">
  <h4>Add exercise to workout</h4>
  <%= render 'exercises/form'%>
</div>
